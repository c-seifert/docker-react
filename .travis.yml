language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t cseifertiz/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true cseifertiz/docker-react npm run test